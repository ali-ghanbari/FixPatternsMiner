package edu.utdallas.fpm.pattern.rules;

public enum CaseBreakerBreakInsertionRule implements Rule {
    CASE_BREAKER_BREAK_INSERTION_RULE;

    @Override
    public String getId() {
        return this.name();
    }
}
