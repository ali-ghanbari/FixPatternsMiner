FACES-1712 aui:script with position="bottom" does not encode scripts to the JSF2 partial-response during Ajax
(fixed compile failure in 2.1.x branch caused by using an overload of UIComponent.getClientId() that was first added in the JSF 2.0 API)

