ICSFormat: fix interleaved use

Corrected interleaved check to use the source plane interleaved flag
instead of the output interleaved status, as the flag was being used to
index into the soruce plane.

