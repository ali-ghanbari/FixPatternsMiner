Fix the bug in storing the OBDA model in the internal system. The OBDA model
ID is always taken from the ontology ID instead of the ontology default prefix.
In this manner, the system will have a consistent identifier.

