core: Fix ImportVmCommand for snapshot images

Snapshot images should be imported always as active = false, whereas the
active images need to be active = true, so fixing it in ImportVmCommand.

Change-Id: Ief2401cc1c875f9a130b203d4e0e18bd4789e2fc

