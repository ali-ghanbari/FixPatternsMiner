remove parameterized type from get() method in EventProvider -- bugfix to code generator broke this. The code generator was not properly parameterizing types. It is now, which means for this to work correctly it can not have a type parameter.

