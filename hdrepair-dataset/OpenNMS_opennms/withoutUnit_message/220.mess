Added "127.0.0.1" to the vhost list for the Jetty unit test server (this
allows you to connect to "localhost" or "127.0.0.1" and get the same
content). Also added opennms-rrd-tcp dependency to the main assembly to
fix Jira bug EXP-9.

