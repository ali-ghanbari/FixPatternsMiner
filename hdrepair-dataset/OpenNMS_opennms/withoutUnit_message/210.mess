Applied patches from bug 3668 comment 16 (Canned early-morning report)
and bug 3745 (PDF availability reports fail to render) to fix issues in
the early morning report and the SVG/PDF availability reports.

