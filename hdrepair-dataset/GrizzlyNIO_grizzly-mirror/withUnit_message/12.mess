[2.2.x] Fix for http://java.net/jira/browse/GRIZZLY-1305 (Client cookie version processing uses an incorrect offset for name start/end).
 - In addition to the fix above, expand coverage on cookie parsing to include byte[] and non-byte[] backed Buffers.

