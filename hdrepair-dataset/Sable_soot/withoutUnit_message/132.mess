bugfix: took three (!) hours to find a bug consisting of one character!!! :-(
LoopNestTree used to "swallow" loops if they were disjoint, due to returning 0 in the comparator.
Note to self: Next time read the docs before using a class form the JDK you have never used before.

