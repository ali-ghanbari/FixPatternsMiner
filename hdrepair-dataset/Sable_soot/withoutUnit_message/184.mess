Temporary workaround: in whole-program mode, process all application classes
rather than just those with reachable methods, since processing only those
with reachable methods is not sufficient. We need to come up with a real fix.

