Fix Permissions.isDisallow in all SDKs

integration.chmod.RolesTest was failing
across the board with ArrayIndexOutOfBounds.
Now that Permissions.restrictions will sometimes
be left null (and therefore translated into
an empty array by Ice) this must be fixed.

