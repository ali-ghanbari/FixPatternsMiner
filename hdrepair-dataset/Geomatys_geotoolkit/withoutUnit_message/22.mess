fix(Renderer) : replace isEmpty() test with Envelopes.containNAN().

Use Envelopes.containNAN() instead of isEmpty() to test request envelope in DefaultRasterSymbolizerRendere to avoid errors when envelope
contain dimension with span value is 0.

