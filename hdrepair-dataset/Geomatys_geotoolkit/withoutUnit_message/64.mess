Looks like that a lot of code specify a null name. Workaround with a replacement of null by "unnamed" for now.
The code specfying a null name will need to be fixed during the migration to SIS classes.

