Check and upgrade maven dependencies (fixes #27)
* set all of dependencies as shared, move every library to ear's lib/ directory
* classpath fixes
* remove unnecessary sun-web.xml and glassfish-web.xml

