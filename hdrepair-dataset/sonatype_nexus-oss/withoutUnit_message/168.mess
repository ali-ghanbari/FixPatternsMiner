NEXUS-4041: not a fix but improvement, not doing full reindex when repo is created (is nonsense to do that, since repo is just created, there is nothing to purge, but in case of groups, this also has wrong consequences)

git-svn-id: file:///opt/svn/repositories/sonatype.org/nexus/trunk@7700 2aa8b3fc-8ebb-4439-a84f-95066eaea8ab

