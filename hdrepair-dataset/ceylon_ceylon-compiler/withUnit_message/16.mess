tests: Encapsulate the CompilerTest.path field with a getPackagePath()
getter, so that we can control the effective compile -src option.

Use this to fix the PackageIssuesTest which I broke yesterday in the issues
tests rearrangement.

