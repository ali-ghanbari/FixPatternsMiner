Add service day to /stoptimes/{date} API call.

This also fix #1791, returning tripId alongside the stoptime.
The advantage is having the same json returned from the thr two API calls
- stops/{stop}/stoptimes
- stops/{stop}/stoptimes/{date}

