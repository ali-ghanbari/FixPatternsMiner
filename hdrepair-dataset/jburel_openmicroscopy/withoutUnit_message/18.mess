Revert "Compile err in XMLMapper"

This reverts commit 3df2bdc8dc0b637a501f13f3706ff81f8c1db628.

The initial need to make this change must have been caused by
a stale build environment. On a clean checkout the compile err
"fix" causes the build to fail, so reverting.

