reabsing branch exp-32 onto master for bugfix release preparation
=================================================================
- Fix for https://www.objecthunter.net/jira/browse/EXP-32
- changed call to 'String.isEmpty()' to a length() check in order to fix a MethodNotFoundError in Android < 2.3
- updated changes.xml file for release preparation

