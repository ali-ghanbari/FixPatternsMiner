Documentation fix for the constructor javadoc

Added escaping for ampersands (&) in the constructor javadoc for the CR/LF characters, so the generated HTML does not show the literal characters, and shows the escape sequence.
