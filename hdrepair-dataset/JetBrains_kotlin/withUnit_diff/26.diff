diff --git a/compiler/backend/src/org/jetbrains/jet/codegen/PropertyCodegen.java b/compiler/backend/src/org/jetbrains/jet/codegen/PropertyCodegen.java
index 0bd7d04..b1fc165 100644
--- a/compiler/backend/src/org/jetbrains/jet/codegen/PropertyCodegen.java
+++ b/compiler/backend/src/org/jetbrains/jet/codegen/PropertyCodegen.java
@@ -253,0 +254 @@
+        flags |= (propertyDescriptor.getModality() == Modality.ABSTRACT ? Opcodes.ACC_ABSTRACT : 0);
diff --git a/compiler/testData/codegen/regressions/kt1482.kt b/compiler/testData/codegen/regressions/kt1482.kt
deleted file mode 100644
index b8c3a98..0000000
--- a/compiler/testData/codegen/regressions/kt1482.kt
+++ /dev/null
@@ -1,21 +0,0 @@
-abstract class ClassValAbstract {
-    abstract val a: Int
-
-    class object {
-        val methods = (this as java.lang.Object).getClass()?.getClassLoader()?.loadClass("ClassValAbstract")?.getMethods()
-    }
-}
-
-fun box() : String {
-    for(m in ClassValAbstract.methods) {
-        if(m.sure().getName() == "getA") {
-            if(m.sure().getModifiers() != 1025)
-                return "get failed"
-        }
-        if(m.sure().getName() == "setA") {
-            if(m.sure().getModifiers() != 1025)
-                return "set failed"
-        }
-    }
-    return "OK"
-}
diff --git a/compiler/testData/codegen/regressions/kt1482_2279.kt b/compiler/testData/codegen/regressions/kt1482_2279.kt
new file mode 100644
index 0000000..18e476b
--- /dev/null
+++ b/compiler/testData/codegen/regressions/kt1482_2279.kt
@@ -0,0 +1,21 @@
+abstract class ClassValAbstract {
+    abstract var a: Int
+
+    class object {
+        val methods = (this as java.lang.Object).getClass()?.getClassLoader()?.loadClass("ClassValAbstract")?.getMethods()
+    }
+}
+
+fun box() : String {
+    for(m in ClassValAbstract.methods) {
+        if (m.sure().getName() == "getA") {
+            if(m.sure().getModifiers() != 1025)
+                return "get failed"
+        }
+        if (m.sure().getName() == "setA") {
+            if(m.sure().getModifiers() != 1025)
+                return "set failed"
+        }
+    }
+    return "OK"
+}
diff --git a/compiler/tests/org/jetbrains/jet/codegen/PropertyGenTest.java b/compiler/tests/org/jetbrains/jet/codegen/PropertyGenTest.java
index 3032294..c9be589 100644
--- a/compiler/tests/org/jetbrains/jet/codegen/PropertyGenTest.java
+++ b/compiler/tests/org/jetbrains/jet/codegen/PropertyGenTest.java
@@ -230 +230 @@
-    public void testKt1482() throws Exception {
+    public void testKt1482_2279() throws Exception {
@@ -232 +232 @@
-        blackBoxFile("regressions/kt1482.kt");
+        blackBoxFile("regressions/kt1482_2279.kt");

