Open xdf files with default editor.

Still a tiny bug to fix. We should ensure both diagram and xdf
files will be opened with the same editor. If we try to open both
files, only 1 tab must be displayed (to avoid syncronizuing issues)

