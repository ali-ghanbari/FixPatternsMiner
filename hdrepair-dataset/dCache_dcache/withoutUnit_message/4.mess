common: Add space and dash back to escapeable args characters

A recent patch modified the semantics of a backslash in arguments, but
that broken a number of valid use cases described in unit tests. This
patch fixes those issues (and for once the unit tests actually caught
a bug!).

Target: trunk
Require-notes: no
Require-book: no
Patch: none

