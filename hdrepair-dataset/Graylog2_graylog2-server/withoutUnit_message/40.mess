add http proxy for use with external resources

useful for setups where the individual graylog2 nodes don't have direct internet access
(e.g. to access http json path or do versionchecks)

fixes #422

