diff --git a/stripes/src/net/sourceforge/stripes/validation/NumberTypeConverterSupport.java b/stripes/src/net/sourceforge/stripes/validation/NumberTypeConverterSupport.java
index ec6ffb6..a4026cb 100644
--- a/stripes/src/net/sourceforge/stripes/validation/NumberTypeConverterSupport.java
+++ b/stripes/src/net/sourceforge/stripes/validation/NumberTypeConverterSupport.java
@@ -105 +105,4 @@
-        // Step 3: replace parentheses with negation
+        // Step 3: trim whitespace that might precede or follow currency symbol
+        output = output.trim();
+
+        // Step 4: replace parentheses with negation
diff --git a/tests/src/net/sourceforge/stripes/validation/NumberTypeConverterSupportTest.java b/tests/src/net/sourceforge/stripes/validation/NumberTypeConverterSupportTest.java
index 272e7a8..6d4ae10 100644
--- a/tests/src/net/sourceforge/stripes/validation/NumberTypeConverterSupportTest.java
+++ b/tests/src/net/sourceforge/stripes/validation/NumberTypeConverterSupportTest.java
@@ -60 +60,12 @@
-    @Test(groups="fast")
+    @Test(groups = "fast")
+    public void testCurrencyWithSpace() {
+        Number number = getConverter().parse("$ 57", new ArrayList<ValidationError>());
+        Assert.assertNotNull(number);
+        Assert.assertEquals(number.intValue(), 57);
+
+        number = getConverter().parse("1,999.95 $", new ArrayList<ValidationError>());
+        Assert.assertNotNull(number);
+        Assert.assertEquals(number.doubleValue(), 1999.95);
+    }
+
+    @Test(groups = "fast")

