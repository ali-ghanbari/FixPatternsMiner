#!/bin/sh

for buggyJavaFile in */modifiedFiles/*/old/*.java; do
    fixedJavaFile=$(dirname $(dirname $buggyJavaFile))"/fix/"$(basename $buggyJavaFile)
    echo $(pwd)"/"$buggyJavaFile
    echo $(pwd)"/"$fixedJavaFile
done

