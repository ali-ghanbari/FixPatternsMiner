DefaultRecentFileService: fix NPE

The issue was caused by the recent rename of OpenImage to OpenFile,
since the DefaultRecentFileService has the fully qualified class name of
that command hard-coded at the moment.

