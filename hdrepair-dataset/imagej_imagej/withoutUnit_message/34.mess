ij-legacy: fix CodeHacker#replaceParameterInNew

We decided to count the parameter index from 1, not 0, so we should do
that in the sanity checking before the real parameter replacement, too.

Signed-off-by: Johannes Schindelin <johannes.schindelin@gmx.de>

