AbstractData: avoid deprecated calibration method

This is a quick and dirty fix, hence the FIXME. Ultimately we will need
to serialize axes properly, or else nonlinear axes in particular will be
hosed by the serialization operation.

