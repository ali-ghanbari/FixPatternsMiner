Merge branch 'fix-command-info'

This branch updates CommandInfo to support a more complete wrapping of a
given PluginInfo. In particular, now when commandInfo.loadClass() is
called, it loads the class in its delegate PluginInfo object, for
consistency.

