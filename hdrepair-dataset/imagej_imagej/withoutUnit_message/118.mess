Updater: Brown paper bag fix

This fixes a bug introduced in 34d01ebe(Updater: prefer the update site's
current versions to locally-cached ones). Basically, it would prevent any
shadowing (update sites overriding other update sites' files).

Signed-off-by: Johannes Schindelin <johannes.schindelin@gmx.de>

