Changing the ports of the parent node now works.

You can click the empty space in the network view to select the parent
node. If it has published ports, they will be displayed. Before,
changing one would throw an exception. This is fixed now.

