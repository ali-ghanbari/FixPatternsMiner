fix default name on python interpreter add

When the change was made to strip off the file exetension for windows,
the .X portion of the interpreter path was removed on other platforms.
Fix this by just removing .exe instead of any dot extension.

