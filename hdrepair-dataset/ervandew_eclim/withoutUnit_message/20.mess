fix possible null pointer on starting of embedded of gvim

introduced with the change set a new global variable in vim to indicate
it was started from vimplugin.  I neglected to update the array copy
offset in one place.

