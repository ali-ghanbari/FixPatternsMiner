diff --git a/doc/reference/en/modules/getting-started.xml b/doc/reference/en/modules/getting-started.xml
index b78b6a1..d3fc413 100644
--- a/doc/reference/en/modules/getting-started.xml
+++ b/doc/reference/en/modules/getting-started.xml
@@ -480 +480 @@
-Query query = parser.parse( "Java rocks!" );
+org.apache.lucene.search.Query query = parser.parse( "Java rocks!" );
@@ -505 +505 @@
-Query query = parser.parse( "Java rocks!" );
+org.apache.lucene.search.Query query = parser.parse( "Java rocks!" );
@@ -507,2 +507,2 @@
-// wrap Lucene query in a org.hibernate.Query
-org.hibernate.Query hibQuery = fullTextEntityManager.createFullTextQuery(query, Book.class);
+// wrap Lucene query in a javax.persistence.Query
+javax.persistence.Query persistenceQuery = fullTextEntityManager.createFullTextQuery(query, Book.class);
@@ -511 +511 @@
-List result = hibQuery.list();
+List result = persistenceQuery.getResultList();
@@ -515 +514,0 @@
-
diff --git a/src/java/org/hibernate/search/jpa/FullTextQuery.java b/src/java/org/hibernate/search/jpa/FullTextQuery.java
index af2b52a..2f173e2 100644
--- a/src/java/org/hibernate/search/jpa/FullTextQuery.java
+++ b/src/java/org/hibernate/search/jpa/FullTextQuery.java
@@ -49,2 +49,2 @@
-	 * <code>list().size()</code> because list() if the index is
-	 * not in sync with the database at the time of query.
+	 * <code>getResultList().size()</code> because getResultList()
+	 * may be not in sync with the database at the time of query.

