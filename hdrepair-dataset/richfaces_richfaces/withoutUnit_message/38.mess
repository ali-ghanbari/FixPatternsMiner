page-fragments: fixing Editor#type method

* with new Selenium it did not typed the text actually
* typing is now done with JS
* the sendKeys methods still needs to be used, so various JS events
  work correctly

