FCPPluginConnection: Adapt JavaDoc to rename "Client" -> "Connection"

Class FCPPluginClient will be renamed to FCPPluginClientImpl soon, so
the Logger string should work. The Logger merely checks for a common
prefix, so the "Impl" won't matter.

