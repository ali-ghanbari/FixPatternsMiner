FCPPluginClient: JavaDoc fixes

Use "ATTENTION" instead of <h1> like in the rest of the JavaDoc.
Otherwise, it would look like the following "ATTENTION" is a sub-section
of the <h1>, which it is not.

