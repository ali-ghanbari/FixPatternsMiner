PluginRespirator.getPluginConnectionByID(): JavaDoc fix

It was not visible what "it" meant. "It" meant the client. This was
caused by the fact that FCPPluginConnection was called FCPPluginCLIENT
previously, so it was obvious back then.

