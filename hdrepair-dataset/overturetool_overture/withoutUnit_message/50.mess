Fix for generation of apply expression that incorrectly assumed that when the root of the apply expression has a sequence type then it is an attempt to index a sequence. Prior to this fix it would fail on generating y = conc [op(), op()] when op returns (say) a sequence of nat

