[GEOS-6688] WCS 1.1.1 GetCoverage request fails

This commit adds the needed tests for the GetCoverage request.

It also fixes the WCSTestSupport class and enables all the tests in the
GetCoverageReaderTest class, which were disabled.

