Changes to the way the R client computes the hmac signature

Signatures are now computed using the timesamp in the UTC timezone. This fixes issues with using hmac authentication on Windows machines. 

