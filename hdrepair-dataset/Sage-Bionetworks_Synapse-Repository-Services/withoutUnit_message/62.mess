(1) now that we have decided its okay to hit S3 from bamboo, none of the tests are "staging only"
(2) fixed a bug in a new integration test where a project was created, not deleted, and another was made with the same name


