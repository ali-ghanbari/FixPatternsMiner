Wait slightly longer before triggering the sync point. Should fix DefaultActorFactoryTest failures (or more correctly, make them less likely to happen. Need to reimplement MultithreadedTestCase at some point).

