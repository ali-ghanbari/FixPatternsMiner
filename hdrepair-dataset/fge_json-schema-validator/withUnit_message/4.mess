IdFragment: USER CHANGE: assume "id" fields are never '#'-prefixed

All examples of schemas I have encountered using ids were never prefixed with a
'#', so it is safe to assume this will not be the case.

Signed-off-by: Francis Galiegue <fgaliegue@gmail.com>

