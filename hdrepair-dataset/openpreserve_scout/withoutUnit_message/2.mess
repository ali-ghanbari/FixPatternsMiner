adding pronom v0.0.6 [fixes #165]

the queries had a missing WHERE triple
'?s rdf:type pronom:file-format .'

incrementing version by 1 micro.

adding separate log appender for this adaptor
in the web app

