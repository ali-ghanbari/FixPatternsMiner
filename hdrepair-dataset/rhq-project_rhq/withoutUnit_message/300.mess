fix for JBNADM-2565 - We're going to try to start components in the availability check, if they're not already running. This should allow us to detect that a server has been started since it was last checked and for its children to also get avail checks.

