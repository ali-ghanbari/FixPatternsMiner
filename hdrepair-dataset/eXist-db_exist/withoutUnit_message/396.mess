[bugfix] ConsistencyCheckTask should create full backups, not incremental ones, by default. Due to this, the <recovery consistency-check="yes"/> setting did not work properly.

svn path=/trunk/eXist/; revision=9270

