[bugfix] Added fallback/workaround to avoid "Unsupported execution mode" error. This should not happen because it means that analyze() was never called on the expression, but there are (hard to reproduce) cases where this occurs.

svn path=/trunk/eXist/; revision=14773

