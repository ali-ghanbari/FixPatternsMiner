[bugfix] Added fallback/workaround to avoid "Unsupported execution mode" error. This should not happen because it means that analyze() was never called on the expression, but there are (hard to reproduce) cases where this occurs. port of rev14773


svn path=/stable/eXist-1.4.x/; revision=14980

