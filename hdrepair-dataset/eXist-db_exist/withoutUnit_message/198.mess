[bugfix] Bad dependency check prevented expressions within predicates to be evaluated in single step mode. Example:

//ACT[string-length() eq max(//ACT/string-length())]

svn path=/trunk/eXist/; revision=15518

