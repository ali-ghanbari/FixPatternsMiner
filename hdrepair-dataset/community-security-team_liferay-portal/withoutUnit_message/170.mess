LPS-28674 Ideally, we'd rename all the "view_entries.jsp-folder" to WebKeys.DOCUMENT_LIBRARY_FOLDER, but there are so many ways that "view_entries.jsp*" is used already, I didn't want to touch those. So just allowing for the inconsistency in those places, but at least fixing it  for TRASH_ENTRY.

