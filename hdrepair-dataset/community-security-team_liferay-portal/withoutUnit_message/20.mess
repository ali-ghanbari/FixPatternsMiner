LPS-54424 Add unchecked cast to fix compile error

It could be solved in a type-safe way by adding an explicit type to the
ArrayList constructor call, but `ant format-source` removes all generic
types in constructors.

