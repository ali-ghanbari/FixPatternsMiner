Improved date format for SimpleDateFormat instance initialization and added a test presenting if it works and how it's better than the previous one. Also fixed two other tests as they were failing after date formatting change.

