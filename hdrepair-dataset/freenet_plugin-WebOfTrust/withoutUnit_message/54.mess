Change default activation depth to 10 to fix NPE. This is a valid workaround:
- The 0.4-final-development branch has explicit activation everywhere which will allow a depth of 1
- The object graph of WoT shouldn't have much higher depth than 5 anyway
