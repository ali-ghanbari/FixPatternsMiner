Fix clone() failure shown by unit tests:
- We didn't copy the data array. There shouldn't be any resulting damage because clone() is only being used in unit tests at the moment
- There are further bugs in the clone() functions of IntroductionPuzzle, will fix them in the following commits.
