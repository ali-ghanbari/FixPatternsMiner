fix(engine): subprocess compensation handlers

- fixes that an event subscription is created for subprocesses with
  compensation handlers before the subprocess activity is left

related to CAM-3628

