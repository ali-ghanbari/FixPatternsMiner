fix(engine): cancel all for parallel multi instance

- fixes the detection of the topmost execution that can be cancelled
  under the constraint that with parallel multi-instance, there
  may be concurrent executions without siblings

related to CAM-3866

