fix pattern for variable parts of route to handle multiple regex parts

currently only one part *with regex* can be specified in a route
making variable part regex pattern reluctant gives possibility to specify
more than one variable parts with regex

